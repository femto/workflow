You are currently running <br/>
<%= @step.workflow_instance.workflow_definition.name %><br/>

<%form_tag :controller=>'workflow', :action=>'transit' do %>
  
  <input type=hidden name=workflow_step_id value=<%= @step.id %>>
  <%= yield %>
  <% @step.workflow_step_definition.workflow_transition_definitions.each do |transition| %>
      <input type=Submit value="<%= transition.name %>" name="workflow_transition"/>

  <% end %>
<% end %>